<li>
   {{#link-to 'vacation' vacation.id}}{{vacation.traveler}}'s vacation to {{vacation.location}} in {{vacation.when}} {{/link-to}}
  {{#if isImageShowing}}
    <p><img src={{vacation.image}} alt="Vacation Image" {{action 'imageHide'}}></p>
  {{else}}
    <button{{action 'imageShow'}}>Show image</button>
  {{/if}}
</li>
